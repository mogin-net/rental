version: "3.4"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: grand-bali-luxe
    restart: unless-stopped
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
